我来详细介绍模拟器模式的实现和工作原理。让我先查看相关的核心文件来了解完整的架构。

Read file: emulator/server.py
Read file: start_generator.py
Read file: emulator/message_generator.py
Read file: example_emulator.py
Read file: emulator/enviroment.py
现在我来详细介绍模拟器模式的完整架构和工作原理：

## 模拟器模式详细架构

### 1. **系统架构概览**

模拟器模式是一个完整的网络环境模拟系统，包含三个核心组件：

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   流量生成器     │    │   强化学习智能体   │    │   HTTP服务器     │
│   (8070端口)    │───▶│   (8090端口)    │───▶│   (8080端口)    │
│                 │    │                 │    │                 │
│ • 生成测试流量   │    │ • DDQN智能体    │    │ • 系统监控      │
│ • 模拟用户/机器人 │    │ • 特征提取      │    │ • CPU/内存监控  │
│ • 35%合法用户    │    │ • 决策执行      │    │ • 负载阈值管理  │
│ • 65%恶意机器人  │    │ • 经验学习      │    │                 │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

### 2. **核心组件详解**

#### **A. HTTP服务器 (server.py)**
**功能**：提供系统监控和状态查询服务

**主要特性**：
- **系统监控**：实时监控CPU和内存使用率
- **Prometheus集成**：使用Prometheus客户端进行指标收集
- **资源限制**：限制内存使用（512MB）和CPU亲和性
- **多线程处理**：支持并发客户端连接

**关键代码**：
```python
def update_system_metrics():
    CPU_USAGE.set(psutil.cpu_percent(interval=1))
    MEMORY_USAGE.set(psutil.virtual_memory().percent)

def handle_client(conn, addr):
    if data.strip() == 'get state for features':
        state = f'{CPU_USAGE._value.get()} {MEMORY_USAGE._value.get()}'
        conn.sendall(str(state).encode('utf-8'))
```

#### **B. 流量生成器 (message_generator.py)**
**功能**：生成模拟的网络流量数据

**流量特征**：
- **合法用户流量**（35%）：
  - IP地址范围：`72.166.25.1-200`
  - 消息长度：1-30字符
  - 发送间隔：2秒
  - 行为模式：正常、随机

- **恶意机器人流量**（65%）：
  - IP地址范围：`72.166.25.200-255`
  - 消息长度：1-5字符
  - 发送间隔：1秒
  - 行为模式：高频、短消息

**消息格式**：
```
{消息内容}@@{IP地址}@@{用户类型}
例如：hello@@72.166.25.50@@True
```

#### **C. 强化学习环境 (enviroment.py)**
**功能**：提供标准的Gymnasium强化学习环境

**状态空间**：7维连续向量
```python
observation_space = spaces.Box(low=-np.inf, high=np.inf, shape=(7,), dtype=np.float32)
```

**动作空间**：4个离散动作
```python
action_space = spaces.Discrete(4)
```

**特征提取**：
1. **网络特征**（5维）：
   - `bytes_m`：当前消息字节数
   - `bytes_b`：总字节数
   - `ave`：平均时间间隔
   - `dev`：时间间隔偏差
   - `num_m`：消息数量

2. **系统特征**（2维）：
   - CPU使用率
   - 内存使用率

### 3. **智能决策机制**

#### **A. 动作类型**
```python
class Action(Enum):
    SERVER_RECIEVE_CURRENT = 0          # 接受当前请求
    SERVER_DROP_CURRENT = 1             # 丢弃当前请求
    SERVER_BLOCK_CURRENT_ADDRESS = 2    # 阻止当前IP地址
    SERVER_BLOCK_CURRENT_ADDRESS_GROUP = 3  # 阻止整个IP地址组
```

#### **B. 奖励函数设计**

**服务器过载时**（CPU/内存 > 75%）：
- **真阴性**（正常用户被接受）：`reward = 0`
- **假阳性**（正常用户被阻止）：`reward = -2/(负载比例 × 危险指数)`
- **假阴性**（恶意用户被接受）：`reward = -(负载比例 × 危险指数)`
- **真阳性**（恶意用户被阻止）：`reward = 负载比例 × 危险指数`

**服务器正常时**：
- **真阴性**：`reward = 0`
- **假阳性**：`reward = -2`
- **假阴性**：`reward = -1`
- **真阳性**：`reward = 1`

#### **C. 多目标动作奖励**
对于阻止IP地址组的动作，系统会：
1. 计算同一IP组内的所有请求
2. 统计真阳性（阻止的恶意用户）和假阳性（阻止的正常用户）
3. 综合奖励：`reward = tp - 2 × fp`

### 4. **训练流程**

#### **A. 环境初始化**
```python
def reset(self):
    # 清空缓冲区
    self.request_buffer = []
    # 填充25个初始请求
    for _ in range(25):
        self.get_state()
    return self.request_buffer[0][1]
```

#### **B. 训练循环**
```python
for episode in range(2000):  # 2000个训练回合
    state = env.reset()
    while not terminated:
        env.get_state()           # 获取新状态
        action = agent.choose_action(state)  # 选择动作
        new_state, reward, terminated, info = env.step(action)  # 执行动作
        agent.save(state, action, reward, new_state, terminated)  # 保存经验
        if steps % 4 == 0:  # 每4步学习一次
            agent.learn()
```

### 5. **网络通信协议**

#### **A. 代理服务器**
- **监听端口**：8090
- **功能**：接收流量生成器的消息，转发给强化学习智能体

#### **B. 服务器通信**
- **目标端口**：8080
- **协议**：TCP Socket
- **消息格式**：
  - 获取状态：`"get state for features"`
  - 获取使用率：`"get server usage"`
  - 响应格式：`"CPU使用率 内存使用率"`

### 6. **系统监控和日志**

#### **A. 训练日志**
- **文件名格式**：`training_log_YYYYMMDD_HHMMSS.txt`
- **记录内容**：
  - 回合进度和时间
  - 当前得分和平均得分
  - 预期完成时间

#### **B. 系统指标**
- **CPU使用率**：实时监控
- **内存使用率**：实时监控
- **负载阈值**：0.75（可配置）
- **危险指数**：1.0（可配置）

### 7. **启动和运行**

#### **A. 一键启动**
```bash
python quick_start.py emulator
```

#### **B. 手动启动（3个终端）**
```bash
# 终端1：启动HTTP服务器
python emulator/server.py

# 终端2：启动训练智能体
python example_emulator.py

# 终端3：启动流量生成器
python start_generator.py
```

### 8. **性能特点**

#### **A. 实时性**
- 毫秒级响应时间
- 支持高并发流量处理
- 实时系统状态监控

#### **B. 可扩展性**
- 模块化设计
- 可配置参数
- 支持自定义特征和动作

#### **C. 鲁棒性**
- 自动重连机制
- 异常处理
- 资源限制保护

模拟器模式提供了一个完整的、真实的网络环境来训练和测试DDoS攻击检测系统，通过真实的网络通信、系统监控和智能决策，实现了接近生产环境的训练效果。