我来详细解释这三个端口服务的功能和它们之间的关系。让我先查看相关的代码来确认具体的实现。

Read file: emulator/message_generator.py
Read file: emulator/server.py
Read file: emulator/enviroment.py
现在我来详细解释这三个端口服务的功能和它们之间的关系：

## 三个端口服务的详细说明

### 1. **端口8070 - 流量生成器 (Traffic Generator)**

#### **功能**：
- **模拟网络流量**：生成模拟的HTTP请求数据
- **用户行为模拟**：区分合法用户和恶意机器人
- **流量模式控制**：控制请求频率和内容

#### **具体实现**：
```python
# 默认配置
host='127.0.0.1'
port=8070
agent_host='127.0.0.1'  # 目标：8090端口
agent_port=8090
```

#### **流量特征**：
- **合法用户流量**（35%）：
  - IP地址范围：`72.166.25.1-200`
  - 消息长度：1-30字符
  - 发送间隔：2秒
  - 行为模式：正常、随机

- **恶意机器人流量**（65%）：
  - IP地址范围：`72.166.25.200-255`
  - 消息长度：1-5字符
  - 发送间隔：1秒
  - 行为模式：高频、短消息

#### **消息格式**：
```
{消息内容}@@{IP地址}@@{用户类型}
例如：hello@@72.166.25.50@@True
```

### 2. **端口8080 - HTTP服务器 (System Monitor)**

#### **功能**：
- **系统监控**：实时监控CPU和内存使用率
- **状态查询**：提供系统状态查询接口
- **资源限制**：限制内存使用和CPU亲和性

#### **具体实现**：
```python
# 默认配置
host='127.0.0.1'
port=8080
memory_limit=512*1024*1024  # 512MB内存限制
```

#### **监控指标**：
- **CPU使用率**：实时CPU使用百分比
- **内存使用率**：实时内存使用百分比
- **Prometheus集成**：支持监控指标导出

#### **API接口**：
- `get state for features`：获取特征提取用的系统状态
- `get server usage`：获取服务器使用率

#### **响应格式**：
```
CPU使用率 内存使用率
例如：45.2 67.8
```

### 3. **端口8090 - 强化学习智能体 (DDQN Agent)**

#### **功能**：
- **代理服务器**：接收来自8070端口的流量
- **特征提取**：从网络流量中提取7维特征
- **智能决策**：使用DDQN模型进行动作选择
- **动作执行**：根据决策执行相应的防御动作

#### **具体实现**：
```python
# 默认配置
proxy_host='127.0.0.1'
proxy_port=8090  # 监听端口
server_host='127.0.0.1'
server_port=8080  # 连接目标
```

#### **特征提取**：
1. **网络特征**（5维）：
   - 当前消息字节数
   - 总字节数
   - 平均时间间隔
   - 时间间隔偏差
   - 消息数量

2. **系统特征**（2维）：
   - CPU使用率
   - 内存使用率

#### **决策动作**：
- `SERVER_RECIEVE_CURRENT` (0)：接受当前请求
- `SERVER_DROP_CURRENT` (1)：丢弃当前请求
- `SERVER_BLOCK_CURRENT_ADDRESS` (2)：阻止当前IP地址
- `SERVER_BLOCK_CURRENT_ADDRESS_GROUP` (3)：阻止整个IP地址组

## 服务之间的关系和数据流

### **数据流程图**：
```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   流量生成器     │    │   强化学习智能体   │    │   HTTP服务器     │
│   (8070端口)    │───▶│   (8090端口)    │───▶│   (8080端口)    │
│                 │    │                 │    │                 │
│ • 生成测试流量   │    │ • 接收流量      │    │ • 系统监控      │
│ • 模拟用户/机器人 │    │ • 特征提取      │    │ • 状态查询      │
│ • 发送到8090    │    │ • 智能决策      │    │ • 返回状态      │
│ • 消息格式:      │    │ • 执行动作      │    │ • 响应格式:     │
│   msg@@ip@@type │    │ • 转发到8080    │    │   cpu mem       │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

### **详细交互过程**：

#### **1. 流量生成阶段**
```python
# 8070端口生成流量
message = f'{message}@@{ip_address}@@{is_user}'
_user_server.sendall(message.encode('utf-8'))  # 发送到8090端口
```

#### **2. 智能体接收和处理**
```python
# 8090端口接收流量
conn, addr = self.proxy_server.accept()  # 接受8070的连接
data = conn.recv(1024).decode('utf-8')   # 接收消息
data, addr, is_user = data.split('@@')   # 解析消息格式

# 特征提取
self.state_data = self.features.extract(data, interval, addr[0])

# 获取系统状态
self.server.sendall(b'get state for features')  # 向8080发送查询
cpu_usage, memory_usage = self.server.recv(1024).decode('utf-8').split(' ')
self.state_server = np.array([cpu_usage, memory_usage])

# 组合特征
state = np.hstack((self.state_data, self.state_server))  # 7维特征向量
```

#### **3. 智能决策和执行**
```python
# 使用DDQN模型选择动作
action = agent.choose_action(state)

# 执行动作
if action == Action.SERVER_RECIEVE_CURRENT.value:
    self.server.sendall(self.current_data.encode('utf-8'))  # 转发到8080
elif action == Action.SERVER_DROP_CURRENT.value:
    pass  # 丢弃请求
elif action == Action.SERVER_BLOCK_CURRENT_ADDRESS.value:
    self.blocked_ips.add(addr)  # 阻止IP
elif action == Action.SERVER_BLOCK_CURRENT_ADDRESS_GROUP.value:
    self.blocked_ip_blocks.add(netmask)  # 阻止IP组
```

#### **4. 系统状态监控**
```python
# 8080端口处理查询
if data.strip() == 'get state for features':
    state = f'{CPU_USAGE._value.get()} {MEMORY_USAGE._value.get()}'
    conn.sendall(str(state).encode('utf-8'))
```

### **端口配置和依赖关系**：

#### **启动顺序**：
1. **8080端口**（HTTP服务器）：首先启动，提供系统监控服务
2. **8090端口**（智能体）：其次启动，连接到8080端口
3. **8070端口**（流量生成器）：最后启动，连接到8090端口

#### **配置参数**：
```python
# 8070端口配置
start(host='127.0.0.1', port=8070, agent_host='127.0.0.1', agent_port=8090)

# 8090端口配置
TrafficEnv(proxy_host='127.0.0.1', proxy_port=8090, 
          server_host='127.0.0.1', server_port=8080)

# 8080端口配置
start(host='127.0.0.1', port=8080)
```

### **网络拓扑**：
```
Internet/Client
    │
    ▼
┌─────────────┐
│ 流量生成器   │ ← 8070端口 (模拟外部流量)
│ (8070)      │
└─────────────┘
    │
    ▼
┌─────────────┐
│ 强化学习智能体 │ ← 8090端口 (代理服务器)
│ (8090)      │
└─────────────┘
    │
    ▼
┌─────────────┐
│ HTTP服务器   │ ← 8080端口 (后端服务)
│ (8080)      │
└─────────────┘
```

### **关键特点**：

1. **分层架构**：每个端口负责不同的功能层
2. **数据流向**：8070 → 8090 → 8080
3. **状态反馈**：8080 → 8090（系统状态）
4. **决策执行**：8090 → 8080（动作执行）
5. **实时监控**：所有组件都有日志和状态监控

这种设计模拟了真实的网络环境，其中：
- **8070端口**模拟外部攻击者和正常用户
- **8090端口**模拟网络防火墙/IDS系统
- **8080端口**模拟受保护的后端服务器